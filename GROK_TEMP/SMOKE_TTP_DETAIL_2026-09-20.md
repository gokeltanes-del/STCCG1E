# Smoke — Detail name + Time Travel Pod

## Detail
TTP / Horga / Tox Detail: Name **1×** in DetailName; Type-Zeile nur `Artifact` (keine Affil-from-Name).

## TTP
1. Earn/play → Pod **links** der Spaceline mit Gap (nicht Table-rechts).
2. Ask Yes/No opponent relocate → Yes: yellow glow board-pick opponent ship (no name dialog).
3. Pod click → **Relocate own ship** → board-pick own ships (once).
4. Discard Pod → ship returns to former location.